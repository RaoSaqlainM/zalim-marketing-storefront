import { useAuth } from "@/_core/hooks/useAuth";
import ProductCard from "@/components/ProductCard";
import StorefrontLayout from "@/components/StorefrontLayout";
import { BackLink, BreadCrumbs, QuantityControl, SectionHeading } from "@/components/CommerceUI";
import { Button } from "@/components/ui/button";
import { startLogin } from "@/const";
import { discountedPercent, formatCurrency, productImage, type StoreProduct } from "@/lib/store";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Check, ChevronDown, CircleCheck, Loader2, MessageCircle, PackageCheck, ShieldCheck, Truck } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { Link, useLocation } from "wouter";

type DetailProduct = StoreProduct & { images: Array<{ id: number; url: string; altText: string | null; position: number }> };

export default function ProductDetail() {
  const [location] = useLocation();
  const slug = location.split("/").at(-1) || "";
  const productQuery = trpc.catalog.bySlug.useQuery({ slug });
  const featured = trpc.catalog.featured.useQuery();
  const { isAuthenticated } = useAuth();
  const utils = trpc.useUtils();
  const [quantity, setQuantity] = useState(1);
  const [activeImage, setActiveImage] = useState(0);
  const [descriptionOpen, setDescriptionOpen] = useState(true);
  const add = trpc.cart.add.useMutation({ onSuccess: () => { utils.cart.get.invalidate(); toast.success("Added to your basket."); }, onError: error => toast.error(error.message) });
  if (productQuery.isLoading) return <StorefrontLayout><div className="container py-10 sm:py-14"><div className="h-[35rem] animate-pulse bg-slate-100" /></div></StorefrontLayout>;
  const product = productQuery.data as DetailProduct | null;
  if (!product) return <StorefrontLayout><main className="container py-12 sm:py-16"><BackLink href="/shop" label="Back to catalogue" /><h1 className="mt-8 font-display text-5xl font-bold tracking-[-.06em] text-slate-950">That product is unavailable.</h1><p className="mt-4 max-w-xl leading-7 text-slate-600">The item you requested may no longer be part of the current Zalim-Marketing catalogue.</p><Link href="/shop" className="mt-7 inline-flex h-11 items-center bg-slate-950 px-5 text-sm font-bold text-white">Explore the catalogue <ArrowRight className="ml-2 h-4 w-4" /></Link></main></StorefrontLayout>;
  const gallery = product.images?.length ? product.images.map(image => image.url) : [productImage(product)];
  const sale = discountedPercent(product);
  const vehicleExamples = String(product.specifications?.VehicleExamples || "Review the technical specification before ordering.");
  const enquiry = encodeURIComponent(`Hello Saqlain, I would like to ask about ${product.name} (${product.sku}). I am interested in ${quantity} item${quantity === 1 ? "" : "s"}. My vehicle is: `);
  const supportLink = `https://wa.me/923255531155?text=${enquiry}`;
  return <StorefrontLayout><main className="container py-8 sm:py-12"><BreadCrumbs items={[{ label: "Home", href: "/" }, { label: "Catalogue", href: "/shop" }, ...(product.category ? [{ label: product.category.name, href: `/collections/${product.category.slug}` }] : []), { label: product.name }]} /><section className="mt-8 grid gap-8 lg:grid-cols-[minmax(0,1.06fr)_minmax(23rem,.94fr)] lg:gap-14"><div><div className="grid gap-3 sm:grid-cols-[5.5rem_1fr]"><div className="order-2 flex gap-2 sm:order-1 sm:flex-col">{gallery.map((image, index) => <button key={`${image}-${index}`} onClick={() => setActiveImage(index)} aria-label={`Show product image ${index + 1}`} aria-pressed={activeImage === index} className={`overflow-hidden border-2 ${activeImage === index ? "border-[#b88d3c]" : "border-slate-200"}`}><img src={image} alt="" loading="lazy" decoding="async" className="aspect-square w-16 object-cover sm:w-full" /></button>)}</div><div className="order-1 overflow-hidden border border-slate-200 bg-[#f3f0e8] sm:order-2"><img src={gallery[activeImage]} alt={product.name} decoding="async" className="aspect-[.98] w-full object-cover" /></div></div><div className="mt-3 grid grid-cols-3 gap-3 border-t border-slate-200 pt-3"><div className="flex gap-2"><Truck className="mt-0.5 h-4 w-4 shrink-0 text-[#9a7027]" /><p className="text-[11px] leading-4 text-slate-600"><b className="block text-slate-950">Practical delivery</b>Discuss timing and destination before confirming.</p></div><div className="flex gap-2"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-[#9a7027]" /><p className="text-[11px] leading-4 text-slate-600"><b className="block text-slate-950">Clear information</b>Check the item details and fit notes first.</p></div><div className="flex gap-2"><MessageCircle className="mt-0.5 h-4 w-4 shrink-0 text-[#9a7027]" /><p className="text-[11px] leading-4 text-slate-600"><b className="block text-slate-950">Need help?</b>Contact us for a fit or delivery question.</p></div></div></div><article className="lg:pt-1"><div className="flex flex-wrap items-center gap-2"><p className="eyebrow">{product.brand?.name || "Zalim selection"}</p>{product.isNew && <span className="bg-slate-950 px-2 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-white">New</span>}</div><h1 className="mt-4 font-display text-5xl font-bold leading-[.9] tracking-[-.065em] text-slate-950 sm:text-6xl">{product.name}</h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-600">{product.shortDescription}</p><div className="mt-7 flex items-end gap-3 border-y border-slate-200 py-5"><span className="font-display text-4xl font-bold tracking-[-.045em] text-slate-950">{formatCurrency(product.price)}</span>{product.compareAtPrice && <span className="mb-1 text-sm text-slate-400 line-through">{formatCurrency(product.compareAtPrice)}</span>}{sale && <span className="mb-1 bg-[#f2e7c5] px-2 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-[#78551b]">Save {sale}%</span>}</div><div className="border-b border-slate-200 py-5"><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[#9a7027]">Vehicle fit and compatibility</p><p className="mt-2 text-sm leading-6 text-slate-700">{vehicleExamples}</p><p className="mt-2 text-xs leading-5 text-slate-500">Vehicle Finder narrows your browse; always compare the product specifications, vehicle dimensions and installation requirements before sending an enquiry.</p></div><div className="mt-5 flex items-center gap-2 text-sm font-semibold text-slate-800">{product.stockQuantity > 0 ? <><CircleCheck className="h-4 w-4 text-[#7d9a42]" />Available to request · {product.stockQuantity} currently listed</> : <><span className="h-2 w-2 bg-red-600" />Currently unavailable</>}</div><div className="mt-6 grid gap-3 sm:grid-cols-[auto_1fr]"><QuantityControl value={quantity} max={Math.max(1, product.stockQuantity)} onChange={setQuantity} /><Button className="h-12 rounded-none bg-slate-950 text-white hover:bg-[#b88d3c]" disabled={product.stockQuantity < 1 || add.isPending} onClick={() => isAuthenticated ? add.mutate({ productId: product.id, quantity }) : startLogin()}>{add.isPending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <PackageCheck className="mr-2 h-4 w-4" />}{product.stockQuantity < 1 ? "Currently unavailable" : "Add to basket"}</Button></div><a href={supportLink} target="_blank" rel="noreferrer" className="mt-3 flex h-12 items-center justify-center border border-[#b88d3c] text-sm font-bold text-[#7b591d] transition hover:bg-[#f9f4e5]"><MessageCircle className="mr-2 h-4 w-4" />Ask about fit or delivery on WhatsApp</a><p className="mt-3 text-center text-xs leading-5 text-slate-500">Zalim-Marketing takes a product enquiry first. No payment details are collected on this website.</p><div className="mt-7 border-t border-slate-200"><button className="flex w-full items-center justify-between py-5 text-left font-bold text-slate-950" onClick={() => setDescriptionOpen(current => !current)}>Product detail <ChevronDown className={`h-4 w-4 transition ${descriptionOpen ? "rotate-180" : ""}`} /></button>{descriptionOpen && <p className="max-w-xl pb-5 text-sm leading-7 text-slate-600">{product.description}</p>}</div><div className="border-t border-slate-200"><p className="py-5 font-bold text-slate-950">Technical specification</p><dl className="grid grid-cols-2 gap-x-6 gap-y-3 pb-6 text-sm">{Object.entries(product.specifications || {}).map(([key, value]) => <div className="border-b border-slate-200 pb-2" key={key}><dt className="text-xs text-slate-500">{key}</dt><dd className="mt-1 font-semibold text-slate-800">{String(value)}</dd></div>)}</dl></div></article></section><section className="mt-16 border-t border-slate-200 pt-12"><SectionHeading eyebrow="Complete the journey" title="More useful essentials" action={{ label: "View full catalogue", href: "/shop" }} /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{((featured.data || []) as StoreProduct[]).filter(item => item.id !== product.id).slice(0, 4).map(item => <ProductCard key={item.id} product={item} />)}</div></section></main></StorefrontLayout>;
}
