import { useAuth } from "@/_core/hooks/useAuth";
import { EmptyState, QuantityControl } from "@/components/CommerceUI";
import StorefrontLayout from "@/components/StorefrontLayout";
import { Button } from "@/components/ui/button";
import { guestBasketChangedEvent, guestBasketSubtotal, readGuestBasket, removeGuestBasketItem, type GuestBasketItem, updateGuestBasketQuantity } from "@/lib/guestBasket";
import { formatCurrency } from "@/lib/store";
import { ArrowRight, MessageCircle, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { Link } from "wouter";

export default function Cart() {
  const [items, setItems] = useState<GuestBasketItem[]>(() => readGuestBasket());
  useEffect(() => { const sync = () => setItems(readGuestBasket()); window.addEventListener(guestBasketChangedEvent, sync); return () => window.removeEventListener(guestBasketChangedEvent, sync); }, []);
  const subtotal = guestBasketSubtotal(items);
  const updateItem = (productId: number, quantity: number) => setItems(updateGuestBasketQuantity(productId, quantity));
  const removeItem = (productId: number) => setItems(removeGuestBasketItem(productId));
  return <StorefrontLayout><main className="container py-10 sm:py-14"><p className="eyebrow">Guest basket · saved in this browser</p><h1 className="mt-3 font-display text-5xl font-bold tracking-[-.065em] text-slate-950 sm:text-7xl">Your basket</h1><p className="mt-4 max-w-2xl leading-7 text-slate-600">Review the parts you are considering. You can start the test checkout without an account; stock, delivery and real payment are always confirmed separately.</p>{!items.length ? <div className="mt-9"><EmptyState title="Your basket is currently empty." text="Browse the Zalim-Marketing catalogue and add useful items for the next drive." action={{ label: "Explore products", href: "/shop" }} /></div> : <section className="mt-10 grid gap-7 lg:grid-cols-[1fr_22rem]"><div className="border border-slate-200 bg-white px-5 sm:px-7"><div className="hidden grid-cols-[1fr_auto_auto] gap-6 border-b border-slate-200 py-4 text-[10px] font-bold uppercase tracking-[.14em] text-slate-500 sm:grid"><span>Product</span><span>Quantity</span><span>Line total</span></div><div className="divide-y divide-slate-200">{items.map(item => <article className="grid gap-4 py-5 sm:grid-cols-[1fr_auto_auto] sm:items-center sm:gap-6" key={item.productId}><div className="flex min-w-0 gap-4"><Link href={`/products/${item.slug}`}><img src={item.imageUrl || "/manus-storage/category-utility_fa340543.jpg"} alt="" loading="lazy" decoding="async" className="h-24 w-24 border border-slate-200 object-cover sm:h-28 sm:w-28" /></Link><div className="min-w-0"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-slate-500">{item.sku}</p><Link href={`/products/${item.slug}`} className="mt-1 block max-w-sm font-bold tracking-tight text-slate-950 hover:text-[#8a601d]">{item.name}</Link><p className="mt-2 text-sm font-semibold text-slate-800">{formatCurrency(item.price)}</p><button onClick={() => removeItem(item.productId)} className="mt-3 inline-flex items-center gap-1.5 text-xs font-bold text-slate-500 hover:text-red-700"><Trash2 className="h-3.5 w-3.5" />Remove item</button></div></div><QuantityControl compact value={item.quantity} max={20} onChange={quantity => updateItem(item.productId, quantity)} /><p className="text-right text-base font-bold text-slate-950">{formatCurrency(item.price * item.quantity)}</p></article>)}</div></div><aside className="h-fit border border-slate-900 bg-slate-950 p-6 text-white lg:sticky lg:top-28"><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[#dfc47e]">Guest test-order summary</p><p className="mt-3 font-display text-3xl font-bold leading-none tracking-[-.045em]">{items.length} selected item{items.length === 1 ? "" : "s"}</p><div className="mt-6 border-y border-white/15 py-5"><div className="flex justify-between text-sm text-slate-300"><span>Product subtotal</span><span>{formatCurrency(subtotal)}</span></div><div className="mt-3 flex justify-between text-sm font-bold text-white"><span>Delivery estimate</span><span>Confirmed by request</span></div></div><p className="mt-5 text-xs leading-5 text-slate-300">This browser-local basket enables a polished test checkout. No payment is collected and no delivery is booked until the team confirms the request.</p><Button asChild className="mt-6 h-12 w-full rounded-none bg-[#d6af59] text-slate-950 hover:bg-[#ead292]"><Link href="/checkout">Continue as guest <ArrowRight className="ml-2 h-4 w-4" /></Link></Button><Link href="/shop" className="mt-5 flex items-center justify-center gap-2 text-xs font-bold text-slate-300 hover:text-white"><MessageCircle className="h-3.5 w-3.5" />Continue browsing</Link></aside></section>}</main></StorefrontLayout>;
}
